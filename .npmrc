# Skip native compilation for better-sqlite3
better_sqlite3_from_source=false
better_sqlite3_use_system_sqlite3=false

# Use prebuilt binaries when possible
build_from_source=false

# Set target for electron
target_platform=win32
target_arch=x64
runtime=electron
disturl=https://electronjs.org/headers
cache=./.electron-gyp
